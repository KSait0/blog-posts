import React from 'react';
import Post from '../components/Post';
import AllPosts from '../components/Allposts';

function Pagination() {
    return <div>
                {/* {this.props.posts.map((p, i) => 
                    <div className="wrap">
                        {this.props.p.map(post => <Post {...post} key={post_id} />)}
                    </div> */}
            </div>
}

export default Pagination;